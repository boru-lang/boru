# boru-project.jsonic — the PROSE half of the project knowledge graph:
# candidate facts about the boru-lang/boru repository drawn from its
# documentation. This is one of the two inputs kg/main.boru turns into
# kg/out/graph.json.
#
# THE OTHER HALF IS CODE, AND CODE WINS ON CODE FACTS. kg/gomod.boru
# reads go.work and every go.mod and emits the module inventory, the
# package inventory, and the directed depends_on edges, quoting the
# actual `use` and `require` lines as evidence. So those facts are NOT
# repeated here: this file no longer asserts `<module> part_of repo` or
# module-to-module edges, because prose could only say "eng builds on
# it" — undirected, and drifting the moment a go.mod changes.
#
# What stays here is what only prose knows: which document explains
# which module, what the language's design concepts are, and how the
# shipped artifacts relate.
#
# ONE COUPLING TO RESPECT. A candidate `entity` reference resolves only
# within its own bundle, so any module or package this file makes an
# assertion ABOUT must also be declared here — and its `key`, `type` and
# `label` must match kg/gomod.boru exactly (key `core-go`, type
# `SoftwareModule`, label `core/go module`; packages are
# `<path> package`), or the two declarations fingerprint to two
# different ids and silently split into two entities. The
# kg_module_without_code_evidence validation rule fails the build if
# that ever happens. Module ATTRIBUTES (path, go_module, unit) are owned
# by gomod.boru and deliberately absent below.
#
# Every assertion here quotes its supporting passage. When the
# documentation changes in a PR, update the affected entries and rebuild
# (see kg/README.md).
{
  sources: [
    {
      id: "src:readme"
      kind: "text"
      locator: "README.md"
      title: "boru README"
      retrieved_at: null
      content_hash: "readme-2026-07"
      authority: "primary"
      metadata: { repository: "boru-lang/boru" }
    }
    {
      id: "src:audit"
      kind: "text"
      locator: "design/LANG-ENG-CONTENT-AUDIT.0.md"
      title: "lang/eng content audit — ADR-012 evidence"
      retrieved_at: null
      content_hash: "audit-2026-08"
      authority: "primary"
      metadata: { repository: "boru-lang/boru" }
    }
    {
      id: "src:agents"
      kind: "text"
      locator: "AGENTS.md"
      title: "AGENTS.md agent guide"
      retrieved_at: null
      content_hash: "agents-2026-07"
      authority: "primary"
      metadata: { repository: "boru-lang/boru" }
    }
    {
      id: "src:completeness-review"
      kind: "text"
      locator: "design/checker-compiler-completeness-review.0.md"
      title: "Type checker + bytecode compiler — completeness review"
      retrieved_at: null
      content_hash: "completeness-review-2026-08"
      authority: "primary"
      metadata: { repository: "boru-lang/boru" }
    }
    {
      id: "src:cli-md"
      kind: "text"
      locator: "CLI.md"
      title: "CLI.md subcommand reference"
      retrieved_at: null
      content_hash: "cli-md-2026-08"
      authority: "primary"
      metadata: { repository: "boru-lang/boru" }
    }
    {
      id: "src:decl-grammar"
      kind: "text"
      locator: "design/DECLARATIVE-GRAMMAR.0.md"
      title: "declarative grammar artifact for both parser twins"
      retrieved_at: null
      content_hash: "decl-grammar-2026-08"
      authority: "primary"
      metadata: { repository: "boru-lang/boru" }
    }
    {
      id: "src:coverage-parity"
      kind: "text"
      locator: "design/ENG-COVERAGE-PARITY.0.md"
      title: "eng standalone coverage-parity program"
      retrieved_at: null
      content_hash: "coverage-parity-2026-08"
      authority: "primary"
      metadata: { repository: "boru-lang/boru" }
    }
    {
      id: "src:core-ts-coverage"
      kind: "text"
      locator: "design/CORE-TS-COVERAGE.0.md"
      title: "core/ts coverage program"
      retrieved_at: null
      content_hash: "core-ts-coverage-2026-08"
      authority: "primary"
      metadata: { repository: "boru-lang/boru" }
    }
    {
      id: "src:root-module"
      kind: "text"
      locator: "design/ROOT-MODULE-FEASIBILITY.0.md"
      title: "root module below core and parser, measured"
      retrieved_at: null
      content_hash: "root-module-2026-08"
      authority: "primary"
      metadata: { repository: "boru-lang/boru" }
    }
    {
      id: "src:basic-check-cut"
      kind: "text"
      locator: "design/BASIC-CHECK-CUT.0.md"
      title: "removing basic's dependency on check"
      retrieved_at: null
      content_hash: "basic-check-cut-2026-08"
      authority: "primary"
      metadata: { repository: "boru-lang/boru" }
    }
    {
      id: "src:core-ts-divergences"
      kind: "text"
      locator: "design/CORE-TS-DIVERGENCES.1.md"
      title: "135 measured core-level Go/TS divergences"
      retrieved_at: null
      content_hash: "core-ts-divergences-2026-08"
      authority: "primary"
      metadata: { repository: "boru-lang/boru" }
    }
    {
      id: "src:go-ts-parity"
      kind: "text"
      locator: "design/GO-TS-PARITY.0.md"
      title: "full functional parity on core, parser and basic"
      retrieved_at: null
      content_hash: "go-ts-parity-2026-08"
      authority: "primary"
      metadata: { repository: "boru-lang/boru" }
    }
    {
      id: "src:ts-parity-audit"
      kind: "text"
      locator: "design/TS-PARITY-AUDIT.0.md"
      title: "parser twin parity audit"
      retrieved_at: null
      content_hash: "ts-parity-audit-2026-08"
      authority: "primary"
      metadata: { repository: "boru-lang/boru" }
    }
    {
      id: "src:state-machines"
      kind: "text"
      locator: "design/STATE-MACHINES.0.md"
      title: "general-purpose state machines: the boru:state module"
      retrieved_at: null
      content_hash: "state-machines-2026-08-14"
      authority: "primary"
      metadata: { repository: "boru-lang/boru" }
    }
    {
      id: "src:hot-code-loading"
      kind: "text"
      locator: "design/HOT-CODE-LOADING.0.md"
      title: "hot code loading: the mechanism report and reload design"
      retrieved_at: null
      content_hash: "hot-code-loading-2026-08"
      authority: "primary"
      metadata: { repository: "boru-lang/boru" }
    }
    {
      id: "src:content-addressing"
      kind: "text"
      locator: "design/CONTENT-ADDRESSING.0.md"
      title: "content addressing: identity by hash, and what it would actually take"
      retrieved_at: null
      content_hash: "content-addressing-2026-08"
      authority: "primary"
      metadata: { repository: "boru-lang/boru" }
    }
    {
      id: "src:reload-invalidation"
      kind: "text"
      locator: "design/RELOAD-INVALIDATION.0.md"
      title: "reload invalidation: hot reload with transparent compilation at zero hot-path cost"
      retrieved_at: null
      content_hash: "reload-invalidation-2026-08"
      authority: "primary"
      metadata: { repository: "boru-lang/boru" }
    }
    {
      id: "src:fn-output-sig"
      kind: "text"
      locator: "design/FN-OUTPUT-SIG.0.md"
      title: "fn output sig: the output slot is types, and only types"
      retrieved_at: null
      content_hash: "fn-output-sig-2026-08"
      authority: "primary"
      metadata: { repository: "boru-lang/boru" }
    }
    {
      id: "src:diagnostic-values"
      kind: "text"
      locator: "design/DIAGNOSTIC-VALUES.0.md"
      title: "diagnostic values: a diagnostic names the values it is about"
      retrieved_at: null
      content_hash: "diagnostic-values-2026-08"
      authority: "primary"
      metadata: { repository: "boru-lang/boru" }
    }
    {
      id: "src:function-value-scope"
      kind: "text"
      locator: "design/FUNCTION-VALUE-SCOPE.0.md"
      title: "function value scope: where a fn value's free words resolve"
      retrieved_at: null
      content_hash: "function-value-scope-2026-08"
      authority: "primary"
      metadata: { repository: "boru-lang/boru" }
    }
    {
      id: "src:fn-value-open-work"
      kind: "text"
      locator: "design/FN-VALUE-OPEN-WORK.0.md"
      title: "function values: the open work, re-measured"
      retrieved_at: null
      content_hash: "fn-value-open-work-2026-08"
      authority: "primary"
      metadata: { repository: "boru-lang/boru" }
    }
    {
      id: "src:compile-declaration-model"
      kind: "text"
      locator: "design/COMPILE-DECLARATION-MODEL.0.md"
      title: "the compile declaration model: one general solution, or fifteen special cases?"
      retrieved_at: null
      content_hash: "compile-declaration-model-2026-08"
      authority: "primary"
      metadata: { repository: "boru-lang/boru" }
    }
    {
      id: "src:full-compilation"
      kind: "text"
      locator: "design/FULL-COMPILATION.0.md"
      title: "full compilation: the total lowering design"
      retrieved_at: null
      content_hash: "full-compilation-2026-08"
      authority: "primary"
      metadata: { repository: "boru-lang/boru" }
    }
    {
      id: "src:full-compilation-handoff"
      kind: "text"
      locator: "design/FULL-COMPILATION-HANDOFF.0.md"
      title: "full compilation: handoff for the bind-twin line"
      retrieved_at: null
      content_hash: "full-compilation-handoff-2026-08-30"
      authority: "primary"
      metadata: { repository: "boru-lang/boru" }
    }
    {
      id: "src:session-handover"
      kind: "text"
      locator: "design/SESSION-HANDOVER.0.md"
      title: "session handover for the full-compilation project"
      retrieved_at: null
      content_hash: "session-handover-2026-09-11"
      authority: "primary"
      metadata: { repository: "boru-lang/boru" }
    }
    {
      id: "src:paren-restep-rule"
      kind: "text"
      locator: "design/PAREN-RESTEP-RULE.0.md"
      title: "the paren re-step rule — what actually decides place-vs-apply"
      retrieved_at: null
      content_hash: "paren-restep-rule-2026-08"
      authority: "primary"
      metadata: { repository: "boru-lang/boru" }
    }
    {
      id: "src:o1-relitigation"
      kind: "text"
      locator: "design/O1-RELITIGATION.0.md"
      title: "O1: re-litigating the NUR101 / NUR078 rulings"
      retrieved_at: null
      content_hash: "o1-relitigation-2026-08"
      authority: "primary"
      metadata: { repository: "boru-lang/boru" }
    }
    {
      id: "src:higher-order-functions"
      kind: "text"
      locator: "design/HIGHER-ORDER-FUNCTIONS.0.md"
      title: "higher-order functions: an empirical capability audit"
      retrieved_at: null
      content_hash: "higher-order-functions-2026-08"
      authority: "primary"
      metadata: { repository: "boru-lang/boru" }
    }
    {
      id: "src:style-guide"
      kind: "text"
      locator: "STYLE-GUIDE.md"
      title: "boru style guide: house style for boru source"
      retrieved_at: null
      content_hash: "style-guide-2026-08"
      authority: "primary"
      metadata: { repository: "boru-lang/boru" }
    }
    {
      id: "src:boru-viz"
      kind: "text"
      locator: "design/BORU-VIZ.0.md"
      title: "boru:viz diagram source generation proposal"
      retrieved_at: null
      content_hash: "boru-viz-2026-08"
      authority: "primary"
      metadata: { repository: "boru-lang/boru" }
    }
    {
      id: "src:adr-004-refinement"
      kind: "text"
      locator: "design/ADR-004-REFINEMENT.0.md"
      title: "ADR-004 refinement — argument-handling categories"
      retrieved_at: null
      content_hash: "adr-004-refinement-2026-08-15"
      authority: "primary"
      metadata: { repository: "boru-lang/boru" }
    }
    {
      id: "src:canon-roundtrip"
      kind: "text"
      locator: "design/CANON-ROUNDTRIP.0.md"
      title: "CANON-ROUNDTRIP — canon always round-trips"
      retrieved_at: null
      content_hash: "canon-roundtrip-2026-08-15"
      authority: "primary"
      metadata: { repository: "boru-lang/boru" }
    }
    {
      id: "src:boru-scry"
      kind: "text"
      locator: "design/BORU-SCRY.0.md"
      title: "boru:scry introspection-as-data proposal"
      retrieved_at: null
      content_hash: "boru-scry-2026-08"
      authority: "primary"
      metadata: { repository: "boru-lang/boru" }
    }
    {
      id: "src:go-module-graph"
      kind: "text"
      locator: "design/GO-MODULE-GRAPH.0.md"
      title: "Go module graph and per-module coverage, measured"
      retrieved_at: null
      content_hash: "go-module-graph-2026-08"
      authority: "primary"
      metadata: { repository: "boru-lang/boru" }
    }
    {
      id: "src:module-views"
      kind: "text"
      locator: "design/MODULE-VIEWS.0.md"
      title: "module-provided views and widgets proposal"
      retrieved_at: null
      content_hash: "module-views-2026-08"
      authority: "primary"
      metadata: { repository: "boru-lang/boru" }
    }
    {
      id: "src:boru-infoview"
      kind: "text"
      locator: "design/BORU-INFOVIEW.0.md"
      title: "boru infoview proposal — the stack at the cursor"
      retrieved_at: null
      content_hash: "boru-infoview-2026-08"
      authority: "primary"
      metadata: { repository: "boru-lang/boru" }
    }
    {
      id: "src:type-representation-audit"
      kind: "text"
      locator: "design/TYPE-REPRESENTATION.0.md"
      title: "What a type name denotes — a representation audit"
      retrieved_at: null
      content_hash: "type-representation-audit-2026-08"
      authority: "primary"
      metadata: { repository: "boru-lang/boru" }
    }
    {
      id: "src:type-node-fusion"
      kind: "text"
      locator: "design/TYPE-REPRESENTATION.1.md"
      title: "One object, one seam — the type-node fusion design"
      retrieved_at: null
      content_hash: "type-node-fusion-2026-08"
      authority: "primary"
      metadata: { repository: "boru-lang/boru" }
    }
  ]
  entities: [
    { key: "boru", type: "Concept", label: "boru language"
      attributes: { paradigm: "concatenative, typed, word-based" } }
    { key: "repo", type: "Product", label: "boru-lang/boru repository"
      external_ids: { github: "boru-lang/boru" } }
    { key: "binary", type: "Product", label: "boru binary"
      attributes: { role: "CLI, REPL, type checker, formatter, LSP, registry client, vault, supervisor" } }
    { key: "forward", type: "Concept", label: "Forward arguments" }
    { key: "stack", type: "Concept", label: "Value stack" }
    { key: "describe", type: "Concept", label: "boru describe discovery" }
    { key: "spec", type: "Concept", label: "Executable language spec" }
    # Go modules and packages: declared so the prose assertions below can
    # reference them. Type, label and key MUST match kg/gomod.boru; the
    # attributes come from go.mod and are set there, not here.
    { key: "cmd-go", type: "SoftwareModule", label: "cmd/go module" }
    { key: "lang-go", type: "SoftwareModule", label: "lang/go module" }
    { key: "basic-go", type: "SoftwareModule", label: "basic/go module" }
    { key: "eng-go", type: "SoftwareModule", label: "eng/go module" }
    { key: "core-go", type: "SoftwareModule", label: "core/go module" }
    { key: "check-go", type: "SoftwareModule", label: "check/go module" }
    { key: "compiler-go", type: "SoftwareModule", label: "compiler/go module" }
    { key: "calc-go", type: "SoftwareModule", label: "calc/go module" }
    { key: "wpg", type: "SoftwareModule", label: "wpg module" }
    { key: "parser-go", type: "SoftwareModule", label: "parser/go module" }
    # kg is a boru pipeline, not a Go module — no go.mod, so no code
    # evidence, so it stays a Product.
    { key: "kg", type: "Product", label: "kg knowledge-graph pipeline"
      attributes: { path: "kg" } }
    { key: "lang-spec", type: "Product", label: "lang/spec directory"
      attributes: { path: "lang/spec" } }
    { key: "utils", type: "Product", label: "utils coreutils subset"
      attributes: { path: "utils" } }
    { key: "boru-cli", type: "Product", label: "boru:cli module"
      attributes: { path: "lang/go/modules/cli.boru", language: "boru" } }
    { key: "readme", type: "Document", label: "README.md" }
    { key: "agents-md", type: "Document", label: "AGENTS.md" }
    { key: "tutorial", type: "Document", label: "TUTORIAL.md" }
    { key: "howto", type: "Document", label: "HOWTO.md" }
    { key: "reference", type: "Document", label: "REFERENCE.md" }
    { key: "explanation", type: "Document", label: "EXPLANATION.md" }
    { key: "cli-md", type: "Document", label: "CLI.md" }
    { key: "style-guide", type: "Document", label: "STYLE-GUIDE.md" }
    { key: "adr", type: "Document", label: "ADR.md" }
    { key: "nur", type: "Document", label: "NUR.md" }
    { key: "eng-guide", type: "Document", label: "eng/go/CLAUDE.md" }
    { key: "parser-guide", type: "Document", label: "parser/go/CLAUDE.md" }
    { key: "lang-guide", type: "Document", label: "lang/go/CLAUDE.md" }
    { key: "vault", type: "Concept", label: "secrets vault"
      attributes: { role: "encrypted API-key / token store, driven by the boru vault subcommand" } }
    { key: "vault-wire", type: "Concept", label: "vault wire protocol"
      attributes: { role: "read-only, HashiCorp-style HTTP API for secret provision (boru vault serve), authenticated by capability tokens" } }
    { key: "design-notes", type: "Document", label: "design/ notes" }
    { key: "lang-eng-audit", type: "Document", label: "design/LANG-ENG-CONTENT-AUDIT.0.md" }
    { key: "completeness-review", type: "Document", label: "design/checker-compiler-completeness-review.0.md"
      attributes: { role: "the 2026-08 checker/compiler completeness review and its §9 implementation record — the living index of the HOF-compilation graduations and the remaining frontier work" } }
    { key: "decl-grammar", type: "Document", label: "design/DECLARATIVE-GRAMMAR.0.md"
      attributes: { role: "the shared declarative tabnas grammar artifact (parser/go/grammar.json): contract, loader pair, and the batch-migration state" } }
    { key: "coverage-parity", type: "Document", label: "design/ENG-COVERAGE-PARITY.0.md"
      attributes: { role: "the standalone 100%/100% coverage program for eng/go and eng/ts: the ratcheting gate floors (make cover-gate-eng, make test-ts), the gap inventories, and the staged plans" } }
    { key: "core-ts-coverage", type: "Document", label: "design/CORE-TS-COVERAGE.0.md"
      attributes: { role: "the coverage program for core/ts (the sibling of ENG-COVERAGE-PARITY, one layer down): the staged work list, the ratcheting TS_CORE_GATE_LINES floor, and the measurement that core/spec is a cross-engine SPEC and not a coverage instrument" } }
    { key: "root-module", type: "Document", label: "design/ROOT-MODULE-FEASIBILITY.0.md"
      attributes: { role: "the measured verdict on a shared module below core and parser: Value alone closes over 75.7% of core/go, so the cut as posed is a rename — includes the piecetool -closure method and the priced seam alternatives" } }
    { key: "basic-check-cut", type: "Document", label: "design/BASIC-CHECK-CUT.0.md"
      attributes: { role: "why basic no longer depends on check: the 23 symbols it used, which moved into core as pure primitives, which became a core-owned analysis seam, and the two gates that keep the edge from coming back" } }
    { key: "core-ts-divergences", type: "Document", label: "design/CORE-TS-DIVERGENCES.1.md"
      attributes: { role: "the ten classes of measured core/go-vs-core/ts divergence behind core/spec/divergent.tsv, why the 1808-row crossdiff was blind to all of them, and the hit rate that makes the uncovered surface the place to look" } }
    { key: "go-ts-parity", type: "Document", label: "design/GO-TS-PARITY.0.md"
      attributes: { role: "the Go/TS parity program for core, parser and basic: the three shared TSV corpora and their two-runner rule, the capability table that forces core/ts before basic/ts, the divergence ledger, and the finding that an uncovered branch in one port is where a divergence hides" } }
    { key: "go-module-graph", type: "Document", label: "design/GO-MODULE-GRAPH.0.md"
      attributes: { role: "the measured Go-side snapshot: the module inventory, the direct-require graph and its twelve-edge transitive reduction, and per-module coverage in both columns — the merged ADR-008 gate and each module's own standalone suite" } }
    { key: "ts-parity-audit", type: "Document", label: "design/TS-PARITY-AUDIT.0.md"
      attributes: { role: "the parser twin parity audit: the render defects the stream oracle found, the parser/spec corpus that replaced the self-referential battery, and the open divergences" } }
    { key: "tabnas-upstream-first", type: "Document", label: "design/TABNAS-UPSTREAM-FIRST.0.md"
      attributes: { role: "ADR-014's case study: why a tabnas parser defect is fixed upstream and never behind a boru shim — the five shims the 2026-08-10 upgrade retired, the two-workarounds-deep episode that earned the rule, and the boundary against boru's own grammar-layer divergences" } }
    { key: "tabnas-dot-boundary-report", type: "Document", label: "design/TABNAS-DOT-BOUNDARY-REPORT.0.md"
      attributes: { role: "the file-ready upstream defect report for the last live tabnas shim: both ports' runnable reproduction against the bare dependency, the follow-character table showing the divergence is conditional (0xFF.5 differs, 0xFF.x agrees), and the acceptance criterion that a regression test must cover both" } }
    { key: "state-machines", type: "Document", label: "design/STATE-MACHINES.0.md"
      attributes: { role: "the design RFC for general-purpose state machines: a mixed Go+boru boru:state module (definition/bindings/snapshot split, pure step, thirteen-item semantic freeze, in-definition input classification via classes:/classify:, state_* check diagnostics, service and process hosts) and the argued decision to add words, not syntax — revised against Noble's Forth FSM paper for the tabular lineage the statechart survey had missed" } }
    { key: "module-views", type: "Document", label: "design/MODULE-VIEWS.0.md"
      attributes: { role: "the rich-display sequel BORU-VIZ.0.md deferred: how any module exposes views and widgets of its own semantics — producer words over the viz data contract with kind-tag vocabulary, TUI widgets by composition, the view word's display bundles via open-word extension, and dashboard alignment with DEBUG-MODULE §7" } }
    { key: "boru-viz", type: "Document", label: "design/BORU-VIZ.0.md"
      attributes: { role: "the boru:viz proposal: pure diagram-source generation (Mermaid + DOT, D2 later) from arbitrary data structures — code generation only, written in boru, with the shared graph/tree/trace/schema contract its §3 pins for boru:scry and every other producer" } }
    { key: "adr-004-refinement", type: "Document", label: "design/ADR-004-REFINEMENT.0.md"
      attributes: { role: "ADR-004's reasoning note, PROMOTED 2026-08-15 into the record's four-categories amendment (NUR023, retired): the four argument-handling categories (forward-eligible, mixed-barrier, stack-only, quoting slots), BarrierPos semantics and its five resolution sites, the stack-only closed list with a two-criterion admission test that admits both apply and __casematch, and the composition rationale — measured at 97 intermediate-barrier signatures of 493, distinguished from the 20-word mixed-overload count" } }
    { key: "canon-roundtrip", type: "Document", label: "design/CANON-ROUNDTRIP.0.md"
      attributes: { role: "ADR-015's reasoning: why canon is a VALUE round-trip (deq) rather than a textual fixpoint, why no kind is exempt, the 2026-08-15 measurements showing functions render as a debug spelling keyed on the binding name and Store as a pointer-bearing Go struct dump, NUR031 as the equality prerequisite, and the two-port property gate that lands with a shrinking failure ledger" } }
    { key: "type-representation-audit", type: "Document", label: "design/TYPE-REPRESENTATION.0.md"
      attributes: { role: "the issue #392 / NUR090 audit: the three-layer diagnosis (uniform lattice, 18-shape type-as-value, InstallType's 11 branches and 3 binding strategies), why the minted node is invisible to evaluation, the measured spelling table, the ResolveSigType fix that was built and backed out, and the §6 bring-(b)-and-(c)-onto-(a) recommendation" } }
    { key: "type-node-fusion", type: "Document", label: "design/TYPE-REPRESENTATION.1.md"
      attributes: { role: "the follow-up design: fuse the minted node and the structural body into one object for named types (evaluation pushes the node; aliases adopt; structure recoverable FROM the node), one three-operation seam over every type value, Match+Unify as uniform node Behaviors, the five-stage migration plan, and the semantic deltas to pin — records NUR093 and NUR094" } }
    { key: "boru-scry", type: "Document", label: "design/BORU-SCRY.0.md"
      attributes: { role: "the boru:scry proposal: the mechanism whereby a boru system gains knowledge of itself as plain data — census, per-word, graph, schema and trace words curated over existing engine seams, and the plan for resolving the overlap with boru:debug" } }
    { key: "boru-infoview", type: "Document", label: "design/BORU-INFOVIEW.0.md"
      attributes: { role: "the boru infoview proposal: position-indexed stack display learned from the Lean 4 infoview — carriers at the cursor via a trace-armed Check, actual values via the debugger's ring and replay, LSP-first delivery (inlay hints, hover, code actions, boru/stackAt), then a binary-served panel rendering scry data through viz" } }
    { key: "hot-code-loading", type: "Document", label: "design/HOT-CODE-LOADING.0.md"
      attributes: { role: "the report on boru's hot code loading ability — late binding end to end, uncached file modules, GC-generational old code, fork snapshot semantics, the aless re-anchor precedent, the BEAM comparison — and the reload-as-a-protocol design: a reload word with keep-old-on-failure, supervisor-broadcast propagation, the Plugin.migrate state hook, handler-stack hygiene, a persistent Vm.open sub-engine" } }
    { key: "fn-output-sig", type: "Document", label: "design/FN-OUTPUT-SIG.0.md"
      attributes: { role: "why an fn triple's output slot is always the types the returns must match, never values to splice: the return-by-value sugar classified the slot before parsing it, so the implicit map a `name:Type` pair lowers to was read as a concrete return and appended to the body — a spurious return-count error, or a silently wrong answer with a clean `check --pedantic` when the body's net stack effect was zero; why patching IsSigTypeValue a fourth time was the wrong fix (three prior arms record the identical symptom, and the predicate fails OPEN into a semantic change); the rule that a literal in the output slot is a literal TYPE admitting exactly itself (ADR-010); the String/Atom literal fallback gated on type-name SHAPE so a misspelled `Integr` stays a loud error; the one behaviour lost (the bodiless base case); the declaration-span fix that makes both spellings diagnose alike; the describe/inspect under-report; and the open interpreter-vs-VM divergence on an empty body's frame residual that deleting the sugar made reachable" } }
    { key: "diagnostic-values", type: "Document", label: "design/DIAGNOSTIC-VALUES.0.md"
      attributes: { role: "the proposed rule that a diagnostic names the values it is about rather than describing them — recorded in design, not ADR.md, pending a maintainer ruling: the return-count error reported arithmetic and withheld the one thing identifying the fault, the count-and-values agreement corollary (callers pass exactly the slice the count was taken over), the single-builder byte-identity contract across interpreter, VM and checker, the abbreviate-never-truncate policy and its two nesting levels through diagMaxListHead, and the open sweep across no_signature, the arity errors and describeStackTypes" } }
    { key: "function-value-scope", type: "Document", label: "design/FUNCTION-VALUE-SCOPE.0.md"
      attributes: { role: "the defect report and design for free-word resolution in function values: the interpreter resolves them in the RUNNING module while the bytecode compiler resolves them in the DEFINING one, so a cross-module fn value can silently bind a same-named word in the caller and return a wrong number with boru check clean; the mechanism (FnDefInfo.Registry set at export, honoured on the value path and by the VM, dropped by name dispatch and by the native-callback seam), why closure capture cannot carry it, the phased fix, the tree-wide migration audit measuring 0 migration sites and 0 silent-change sites against 224+ repaired (the cost is nil because the ecosystem already worked around it in writing — 12 utils unroll Cli.main, aless ships a wrapper, sort.aql is one file), the finding that every divergence runner in the ecosystem runs compile-preferring mode in its INTERPRETER column and is structurally blind, and the three-clause target semantics for function values" } }
    { key: "compile-declaration-model", type: "Document", label: "design/COMPILE-DECLARATION-MODEL.0.md"
      attributes: { role: "the proposal for what a word must tell the bytecode recorder, measured rather than argued: boru already has TWO general solutions — interpreter islands (OpFallback), used at scale then ratcheted to islandCeiling 0, and a CompileEffect taxonomy that grew to fifteen flags to replace them. The measurement (declaration counts taken from CompileEffect ASSIGNMENTS, not identifier occurrences, after a first draft inflated every row with the flags own comments): CompileExecutesBody has ZERO declaration sites, a dead member of a vocabulary that only grows; four further per-slot fields carry compile-relevant facts (CallableSpec, FnInertArgs, FnDataArgs, StoredBodies); 153 ledgered frontier rows of which ~43 are fn-value shapes and ~36 provenance, frontier-hof-audit.tsv alone 65 or 43% of the ledger; and SEVEN rows that compile and answer correctly but are ledgered as failures purely because the program embeds an island. The diagnosis: CompileDefault = 0 is not an absence of information but the substantive claim that the handler re-steps fn operands on the tape, false for most words that receive one, and the flags are a patch set withdrawing it one word at a time — the same defect three times (var, service/add, boru:fn-util). The proposal: collapse the ten operand-facing flags to three orthogonal per-position facts (tapeBound, the representation needed, the env where names resolve — the last already reasoned about at emit.go:4827 with no field to hold it), subject to four constraints verified in review (tapeBound must be tri-state because Go zero-values a bool to the PERMISSIVE answer, needs must be a set because stored-body words accept RawTokens OR CompiledUnit, env must be derived per slot because is and as are None where parselang is not, and cross-field validity must be asserted because independence increases the expressible states); and let an island carry a result contract, which is an extension of dynOutNativeOK and ReturnsFn rather than a new mechanism, since an island output already HAS provenance and only lacks a type. What it explicitly does not claim: boru:fn-util is NOT the demonstrator — its rows refuse in resolveDynamicApply on the NUR101 application model, not on an island result type; the benefit of typed islands is unmeasurable until the islandCeiling ban lifts, since no frontier row cites the downstream-of-dynamic refusal; the ~36 provenance rows are a separate axis; and two lanes that must agree is a bug detector, NUR101 having been found BY the disagreement with the compiled lane correct" } }
    { key: "full-compilation", type: "Document", label: "design/FULL-COMPILATION.0.md"
      attributes: { role: "the total-lowering design for full compilation, answering the directive that islands are unacceptable, the interpreter is not an escape hatch, and failure to compile is a hard error: the compiler becomes TOTAL by changing its worst verdict from refuse to lower generically — every dispatch the checker cannot resolve compiles into opcodes that make the interpreter's own decisions at runtime by calling the same kernel routines (one kernel, two lanes: typed where the checker has proof, generic elsewhere, decided per event not per program). The measured corrections to the frame: islandCeiling 0 counts only OpFallback while the compiled lane still re-enters Engine.Run via OpCallDynFrame value windows, the drift window's source-token stepping, the callDynamic non-closure arms and ~15 vmDefer whole-program re-runs; and the 153-row ledger samples ~30 of ~130 distinct refusal reasons, so totality is proven against the gate inventory with generated sweeps. The mechanisms: statement descriptors plus an extracted collection kernel carrying the forward-collection/barrier state that selects between two raise texts (the §9d finding a bare stack window cannot repair); universal open-closure-converted fn values with lazy first-apply compilation; one Apply kernel implementing the NUR101 application model (name-read lead is WORD dispatch through the live def stack, anonymous value is data); bind twins making VM-time def order real; production-order mark regions dissolving the provenance families; the resident compiler for eval-class code with an induction argument for totality through runtime compilation; checker totality by compiling statically-definite errors to traps under an erasure discipline with lump boundaries; and retirement of every vmDefer valve onto compiled landing pads (the Self deopt-to-compiled precedent). Adopts COMPILE-DECLARATION-MODEL's declaration triple, rejects its typed islands (the seven working island rows graduate natively via the universal fn-value lane), keeps the interpreter as the reference oracle so the two-lane bug detector survives, and stages the work behind new ratchets — engineEntryCeiling and deferCeiling — that make the live island residue measurable for the first time" } }
    { key: "full-compilation-handoff", type: "Document", label: "design/FULL-COMPILATION-HANDOFF.0.md"
      attributes: { role: "the RUNNING LOG of the bind-twin line (FULL-COMPILATION.0.md \u00a76.5), recording state and measurement rather than restating design, and appended to as work lands so an earlier section's dated figures stand beside the later one that overtook them \u2014 because what a measurement said BEFORE a fix is half of why the fix is what it is. The twins are LANDED AND FLIPPED (2026-09-01/02): rollback-and-replay is the only regime, the keep-installs path is deleted, and the rollback base rides with the Program (EmitState.BindRegistry snapshots at its first bind, Finalize stamps ReplayBase/ReplayReg, eng.runProgram restores on that registry only, for a program that recorded a transition, from a clone so a program can run twice). The line as logged: the measured population that sized every increment (7644 lang/spec rows, 4290 with transitions, 7441 total \u2014 def 6361 at 85%, type-install 1048, undef 30, def-replace 2, deepest single row 36 not 1672, which reversed the earlier claim that per-transition twin cost is a budget concern), then the staged sequence that must not be inverted (close the live-depth oracle, emit the def twin while installs are still KEPT so it is inert, only then flip), then the flip rehearsal that made the flip a deletion rather than a migration by running every module suite with the flag forced on \u2014 which closed three classes with principled accounts (post-trap twins are unreachable not lost, superseded fixed-point rounds record speculative twins, the sibling multi-run words graduate on the measured mechanism), surfaced NUR116 and bounded NUR115, and left three annotation-only goldens. Then the variation lane's exposure, the population the rehearsal missed because it only runs under make test's 35-minute timeouts: fifteen variants that now refuse soundly where the old default compiled them on kept installs (pinned as one named bucket with a representative frontier row per shape), and one known miscompile graduated. THE PAYOFF GATES, MEASURED (2026-09-02) is the section that matters most for planning: \u00a76.5 named four gates the twins would delete \u2014 the frozen-read refusals, the interim stored-handler latches, family L's conditional fn shadow, NUR037's fn-local-fn refusal \u2014 and ALL FOUR ARE LOAD-BEARING, each for a different reason, under one root: the twins fix WHERE THE REGISTRY IS, and these gates defend against WHAT IS IN THE BYTECODE. Rollback-and-replay does not un-bake a const (frozen-read's stale value is a PUSH_CONST inside a compiled unit, so deleting the gate answers 1 1 for the interpreter's 1 2, silently), does not supply a runtime lookup (the stored-handler fallback still reads the pass-final binding \u2014 that is \u00a76.9's OpDispatchGeneric), cannot replay a transition the check pass never recorded (a rolled-back arm notes none, and the rollback now makes the registry disagree with the baked OpCallUser where the old default had them agree), and never sees fn-body defs at all (excluded BY CONSTRUCTION at FnBodyDepth > 0, a deliberate exclusion worth 69254 ledger entries down to 6110). All four re-file under \u00a76.9's lookup half, family L and NUR037 additionally needing a BINDER half; the design doc carries each correction inline beside the sentence it falsifies, along with the fact that OpCollect and OpDispatchGeneric do not exist (Program.Regions had zero writers and zero readers until Stage 4a gave it the first writer, below). Also here: the LATCH-TO-STACK DESIGN for nested multi-run bodies, which the handoff had been citing for a day without it existing anywhere \u2014 reconstructed from the code and measured, correcting three of the four remembered parts (a stack of OPEN brackets is empty exactly when adoption needs it, so the structure is a persistent tree walked by a second compile-time descent; supersede must compare against the previous SIBLING; owned-set subtraction must subtract superseded ranges as well as child ranges; and the remembered root-fence relaxation names a condition that does not exist there), with fold-nested-multirun shown to be FOUR stacked declines rather than one. Its Part 1, the publication gate (MultiRunBodyGuard publishes only at FnBodyDepth == 0, exactly when a twin can be noted at all), LANDED and graduated two shapes from refusal to parity. Method lessons the line paid for and the next reader should not re-derive: a green gate over a corpus that cannot contain the case is not evidence; a count of call sites is not a count of transitions (a review found 1750 missing transitions, 23% of the population, and one site-table row that was false); -compile falls back silently so only -force-compile can measure a gate; and boru check --emit settles a baked-instruction question before any gate is disabled. STAGE 4a, the region table INERT (2026-09-03), is the line's next section and repeats the twins' own method: Program.Regions gets its first writer while nothing reads it, gated over the whole corpus by TestRegionTableWellFormed. It opens by recording what the target is NOT \u2014 the obvious \"collect the simplest dispatches\" slice was already built once and reverted, because TestEmitSplitFormsIdentical requires the three spellings of one assignment to lower identically and a descriptor carries the split \u2014 so the target is the dispatches the ordinary lowering CANNOT handle, which of \u00a76.5's latches is the frozen-read k pair alone, verified refusing at the CLI and still open as OpCollect's acceptance test. Four things the corpus said that the design did not: Phase B is an INDEX rather than the SEARCH three reverted models built (matching fills sig positions from the forward tokens in written order, so slot i is sig position i and the source is ops[i] \u2014 checked by value identity, never assumed, stopping where the correspondence stops, which is NFwd); a word slot's answer is decided by where its BINDING lives rather than by where the dispatch sits, and the discriminator is the CLOSURE-CAPTURE rule verbatim (Defs.Depth(name) > FnBaselines[top][name]) because captures and descriptors ask the same question \u2014 a frame-bound name takes SlotLocal when the operand names its slot and STOPS the claim otherwise (a body-local def bakes a const, and a computed one is promoted only after completion), while a module-scope name read inside a fn body stays LIVE because that is the k pair OpCollect exists for; of 6361 claimed word-token slots only 848 are live wordRefs and 5513 are frame slots, and the first cut of this rule was wrong twice (keyed on the operand it described body-locals as live; keyed on \"inside a fn unit\" it would have deleted the k pair's own descriptor), both caught in review; a word slot also resolves against the DISPATCH's registry rather than the recorder's es.reg, which after a call into a boru-implemented module is that module's sub-registry; a recorder-side table is not rollback-safe, so the descriptor rides its call EVENT and the LOWERER appends it like DispatchSpec, which removed 55 descriptors belonging to discarded loop-analysis rounds; and the claim is a small prefix of the region (22958 of 65959 span slots, with 17650 descriptors claiming nothing forward at all), which is why NFwd exists and why the alternative measured +9.9%. The table's bound is pinned rather than noted: Phase B is seated on RecordCall alone, 38734 descriptors over 5629 programs \u2014 a fraction of the dispatches Phase A offers a capture to \u2014 and the e2e pin fails if a seat widens without the claim widening with it" } }
    { key: "session-handover", type: "Document", label: "design/SESSION-HANDOVER.0.md"
      attributes: { role: "the CURRENT-STATE page for the full-compilation project, and the one file a fresh session reads first: it says where the gates stand today rather than how they got there, which is the division of labour with FULL-COMPILATION-HANDOFF.0.md — that log is append-only, so an earlier section's dated figures survive beside the later ones that overtook them and it is the wrong place to look for what is true now. Carries the gate table (frontier ledger 29, interp-entry census 29 and failing in BOTH directions, refusal/island/type-soundness pins at 0, minCompiledRows 6410, diagnostic-parity 320, armed-only 4, two remaining twin-placement shapes), what is in flight on the branch, the next increment's three MEASURED constraints with the design they rule out, the ranked alternatives, the instruments that can see a defect the ordinary gates cannot (TestInterpEntryCensus is the only thing that sees a compiled program whose BODY is interpreted, since -force-compile reports SUCCESS for those; TestVariationDifferential runs only under make test and the merged coverage gate), and the process rules this line paid for in red CI — among them that re-running the gate that owns an edit applies to cosmetic edits too (four reds traced to skipping it, one on a commit containing nothing but a design note), that CI green is not the same as gated because the repo-wide ADR-008 floor is a separate dispatchable workflow, that a what-remains-is narrowing inherits the last reader's vantage point, and that a COUNT test may be asked of the probe where a SHAPE test may not" } }
    { key: "paren-restep-rule", type: "Document", label: "design/PAREN-RESTEP-RULE.0.md"
      attributes: { role: "the measured statement of when a paren-collapsed function is PLACED and when it is re-stepped into a CALL, superseding NUR101's 2026-08-26 place-uniformly ruling whose premise it falsifies. The rule: a Function a paren placed is re-stepped exactly when it leads TWO OR MORE survivors of an enclosing group that closes with a paren rewind — a user paren, an fn frame, or an if/for/do body — while the program top level, list literals and map literals do not rewind; placement is the one-survivor case and the enclosing group is a SECOND decision taken one paren out, not a context that modifies the first. The ruling had been implemented by deleting fnReturnPark's survivor-count clause on the reasoning that the count was never the right question; it is, and deleting it turned (x:Integer => [x mul 2] 5) from 10 into fn (Integer) 5 and broke seven suites. The defect was the COMPILER'S, in both directions, and there were five silent miscompiles: (mk 1) 2 and (mk2 5) 10 applied what the interpreter places, while [((mk 1) 2)] and two if-arm shapes placed what it applies — ((mk 1) 2) compiling correctly at the top level only by ACCIDENT, because the outer paren collapses and the pair reaches the program residual where the carrier arm applies it. The fix is a matched PAIR of records taken at the collapse, the last moment the two spellings are distinguishable — ParenPlacedFnIDs and the new ParenReSteppedFnIDs — read by the residual lowering, the branch-arm merge and the list-literal assembly, which is what lets [(mk 1) 2] keep compiling while [((mk 1) 2)], byte-identical in its elements, refuses. Value divergences on the 16-shape probe: five to zero. Also the finding of how they survived a 100%-covered parity suite (NUR106): Stage J flipped lang.Run to the compiled path and 75 parity assertions across five files still read it as their interpreter oracle, comparing the compiled lane against itself; the sweep to RunInterp surfaced NUR107 on the very test that had pinned that claim as non-reproducing" } }
    { key: "o1-relitigation", type: "Document", label: "design/O1-RELITIGATION.0.md"
      attributes: { role: "the re-litigation brief for FULL-COMPILATION open question O1, which blocks Stage 3: both rulings Stage 3 depends on are re-measured rather than re-argued, and both records turn out to be out of date. The finding that actually blocks implementation is that NUR.md holds TWO entries titled NUR101, both Status Pending, both claiming the anchor #nur101, ruling the OPPOSITE way on the same program — line 243 holds the compiled lane correct and the interpreter in need of fixing, line 439 states the compiled lane is the defect — with line 243 recording that line 439's reading was reverted while the superseded record was never deleted, so O1 cannot be implemented as ruled because the register does not record one ruling, and the duplicate anchor makes every #nur101 link resolve to the first. Re-measurement then narrows the question twice: NUR101's top-level half is ALREADY FIXED (all five placement cases place, including the two the record calls defective — (mk 1) 2 answers fn (Integer) 2 where the record says 3), so what remains is not about list literals at all but the single question of whether a COMPUTED function applied inside an enclosing group places or dispatches ((mk 1) 2 places, ((mk 1) 2) dispatches, and the list literal merely inherits it), with ADR-011's carve-out for a bare WORD inside a group neither clearly covering nor excluding a computed group result — which is how two records read it two ways; and NUR078's divergence is confirmed live (h zero answers 42 through the struck TFunction intercept while hany zero is a barrier error, so the slot type decides) but its prescribed replacement names /r, a modifier ADR-011 collapsed into /v, so the ruling as literally written removes the only working path and directs users to one that does not exist. Three rulings are needed to unblock Stage 3: delete one NUR101 entry, decide place-vs-dispatch-vs-context-dependent for the enclosing-group case, and decide implement-as-amended-respelled-to-/v vs reverse-the-amendment for NUR078 — the latter given no recommendation, since the tree pins the exception as designed behaviour in path-modifier.tsv:67 while the register records it as struck" } }
    { key: "higher-order-functions", type: "Document", label: "design/HIGHER-ORDER-FUNCTIONS.0.md"
      attributes: { role: "the empirical audit of higher-order support and combinator expressibility, run against a built binary rather than read from the design notes (a POINT-IN-TIME report, 2026-08-19): what was actually built and run — SKI with I derived as S K K, BCKW, U-combinator anonymous recursion, Church numerals and pairs, CPS, a parser-combinator library, closures over fn-locals that outlive their scope — and the one construction that resisted every spelling, Church and/or; a capability comparison against Haskell, Scheme, JavaScript and Factor; and the call-vs-value gotcha cluster that only surfaces once a function value flows through a PARAMETER: a bare name calls where /r refused so no name-based read was total over both kinds — CLOSED by the 2026-08-19 /r->/v rename that made the value read total (NUR085, retired), the four code-body iterators taking a Function callback over a Map but not a List where filter takes both (NUR086), a branch-local def lost to the checker after a Function-parameter call so boru run refuses a working program (NUR087), the capitalised-name predicate-type collision, the context-dependent application of a paren-collapsed function, and the confirmation that NUR073 reproduces with the PLAIN default invocation siding with the compiler while -no-compile disagrees, exit 0 both ways" } }
    { key: "fn-value-open-work", type: "Document", label: "design/FN-VALUE-OPEN-WORK.0.md"
      attributes: { role: "the open-work inventory for the function-value line after PRs #366/#375/#378: four remaining items, all unblocked since the 2026-08-17 maintainer rulings (§1.1) — clause 3 (parens do not re-step) ruled BROAD, clause 2 (passing a function requires /r) enabled by the ADR-011 amendment with all four engine sites retiring together (re-opening the NUR038 call-head question, registered as NUR078), break 2 (the compiler's sound refusal of a 0-arg fn read from a plain container), and the StackForm apply — ruled a new dedicated Apply Op — every figure re-measured against 8732662, correcting three that the earlier notes carry: the clause-3 row count is 30 not 31, the clause-2 bare-name count is 3 not 9, and narrow-vs-broad ARE separable, by FnDefInfo.Name rather than by a rejected value-borne marker; plus the finding that break 2 is an arity-0 extension of tryMemberFnArrivalDispatch rather than Phase 3 work, that its refusal masks a confirmed miscompile and is untracked by any frontier row, and that the apply Op's sketched target has two holes — a 0-arg anonymous fn is silently not applied, and `apply` itself double-records so a replay applies twice" } }
    { key: "content-addressing", type: "Document", label: "design/CONTENT-ADDRESSING.0.md"
      attributes: { role: "the design note for deriving a definition's identity from its content rather than its name: the three costs that share that root cause (the per-invoke DepsFresh walk, the AOT codec's symbolic-reference refusals, the pre-1.0 rename tax), the split between an ARTIFACT digest over file bytes — unblocked, already specified by boru-vendor §5 — and a DEFINITION digest over meaning, which needs canonicity, alpha normalisation, macro expansion, referent substitution and cycle components; three options for referent substitution under call-time binding, recommending a (text digest, world digest) compound key; a five-phase sequence; and the rejections — codebase-as-database, hash-based type identity, immutable definitions as a language rule. Measured by design/unison-hash-identity-probe.0.md and scripts/hash-identity-probe.sh" } }
    { key: "reload-invalidation", type: "Document", label: "design/RELOAD-INVALIDATION.0.md"
      attributes: { role: "how hot reload and transparent compilation coexist without slowing compiled code: per-ref valid flags flipped push-style through a reverse dependency index (the HotSpot/Truffle/Julia equilibrium), ref unification at Finalize, world-pinned whole-program units, per-world restamp budgets — plus the confirmed F1 pass-hoisting divergence and its interim whole-program-refusal fix" } }
    { key: "org", type: "Organization", label: "boru-lang" }
  ]
  assertions: [
    { subject: "repo", predicate: "created_by"
      object: { kind: "entity", entity: "org" }
      evidence: [{ source: "src:readme", locator: "Install",
        quote: "git clone https://github.com/boru-lang/boru" }]
      confidence: 0.95 }
    { subject: "binary", predicate: "part_of"
      object: { kind: "entity", entity: "cmd-go" }
      evidence: [{ source: "src:readme", locator: "Repository layout",
        quote: "cmd/go/ | The boru CLI / REPL" }]
      confidence: 0.97 }
    # NOTE: `<module> part_of repo` and every module-to-module edge used
    # to live here, quoted from the README layout table. They are now
    # produced by kg/gomod.boru from go.work and go.mod as directed
    # depends_on edges — better evidence, and a direction prose did not
    # carry. Do not re-add them here.
    { subject: "kg", predicate: "part_of"
      object: { kind: "entity", entity: "repo" }
      evidence: [{ source: "src:agents", locator: "Repository layout",
        quote: "kg/ | The project knowledge graph: an evidence-backed boru pipeline and its generated bundle." }]
      confidence: 0.98 }
    { subject: "utils", predicate: "part_of"
      object: { kind: "entity", entity: "repo" }
      evidence: [{ source: "src:agents", locator: "Repository layout",
        quote: "utils/ | A coreutils subset written in boru — real programs that prove the CLI story (argv, exit codes, streams, baked permissions)." }]
      confidence: 0.98 }
    { subject: "utils", predicate: "related_to"
      object: { kind: "entity", entity: "boru-cli" }
      evidence: [{ source: "src:readme", locator: "Repository layout",
        quote: "utils/ | A coreutils subset written in boru (cat, wc, head, grep, …) — real programs, built with boru build." }]
      confidence: 0.95 }
    { subject: "boru-cli", predicate: "part_of"
      object: { kind: "entity", entity: "lang-go" }
      evidence: [{ source: "src:agents", locator: "Repository layout",
        quote: "utils/ | A coreutils subset written in boru — real programs that prove the CLI story (argv, exit codes, streams, baked permissions)." }]
      confidence: 0.9 }
    { subject: "utils", predicate: "related_to"
      object: { kind: "entity", entity: "binary" }
      evidence: [{ source: "src:readme", locator: "Repository layout",
        quote: "real programs, built with boru build" }]
      confidence: 0.9 }
    { subject: "boru", predicate: "has_attribute"
      object: { kind: "literal", value: "typed, word-based query language", datatype: "String" }
      evidence: [{ source: "src:readme", locator: "intro",
        quote: "boru is a typed, word-based query language." }]
      confidence: 0.98 }
    { subject: "boru", predicate: "has_attribute"
      object: { kind: "literal", value: "concatenative", datatype: "String" }
      evidence: [{ source: "src:readme", locator: "intro",
        quote: "Underneath, boru is concatenative: words can equally take their arguments from a value stack" }]
      confidence: 0.98 }
    { subject: "boru", predicate: "related_to"
      object: { kind: "entity", entity: "forward" }
      evidence: [{ source: "src:readme", locator: "Forward arguments",
        quote: "the defining feature of the surface syntax is forward arguments" }]
      confidence: 0.95 }
    { subject: "boru", predicate: "related_to"
      object: { kind: "entity", entity: "stack" }
      evidence: [{ source: "src:readme", locator: "intro",
        quote: "words can equally take their arguments from a value stack, which is what makes point-free pipelines compose" }]
      confidence: 0.95 }
    { subject: "binary", predicate: "supports"
      object: { kind: "entity", entity: "describe" }
      evidence: [{ source: "src:agents", locator: "First: let the tool document itself",
        quote: "The boru CLI documents both the language and itself, and that output is generated from the live engine" }]
      confidence: 0.95 }
    { subject: "lang-spec", predicate: "part_of"
      object: { kind: "entity", entity: "repo" }
      evidence: [{ source: "src:readme", locator: "Repository layout",
        quote: "lang/spec/ | Engine spec TSV files (the language's executable spec)." }]
      confidence: 0.98 }
    { subject: "spec", predicate: "part_of"
      object: { kind: "entity", entity: "lang-spec" }
      evidence: [{ source: "src:agents", locator: "Task router",
        quote: "The executable language spec (the rows tests run against) | lang/spec/*.tsv" }]
      confidence: 0.95 }
    { subject: "readme", predicate: "part_of"
      object: { kind: "entity", entity: "repo" }
      evidence: [{ source: "src:agents", locator: "header",
        quote: "New to the repo? Skim README.md first." }]
      confidence: 0.95 }
    { subject: "agents-md", predicate: "mentions"
      object: { kind: "entity", entity: "describe" }
      evidence: [{ source: "src:agents", locator: "First: let the tool document itself",
        quote: "Before grepping source or guessing a word's signature, ask the binary." }]
      confidence: 0.95 }
    { subject: "tutorial", predicate: "supports"
      object: { kind: "entity", entity: "boru" }
      evidence: [{ source: "src:readme", locator: "Documentation",
        quote: "Tutorial | You are new to boru and want to learn it step by step." }]
      confidence: 0.95 }
    { subject: "howto", predicate: "supports"
      object: { kind: "entity", entity: "boru" }
      evidence: [{ source: "src:readme", locator: "Documentation",
        quote: "How-To Guides | You have a specific task and want a recipe." }]
      confidence: 0.95 }
    { subject: "reference", predicate: "supports"
      object: { kind: "entity", entity: "boru" }
      evidence: [{ source: "src:readme", locator: "Documentation",
        quote: "Reference | You need the precise behaviour of a syntax form, type, or word." }]
      confidence: 0.95 }
    { subject: "explanation", predicate: "supports"
      object: { kind: "entity", entity: "boru" }
      evidence: [{ source: "src:readme", locator: "Documentation",
        quote: "Explanation | You want to understand why boru is the way it is." }]
      confidence: 0.95 }
    { subject: "cli-md", predicate: "supports"
      object: { kind: "entity", entity: "binary" }
      evidence: [{ source: "src:readme", locator: "Documentation",
        quote: "CLI Reference | You want to drive the boru binary from the shell." }]
      confidence: 0.95 }
    { subject: "adr", predicate: "mentions"
      object: { kind: "entity", entity: "boru" }
      evidence: [{ source: "src:readme", locator: "Documentation",
        quote: "Architecture Design Record | You want the key architectural decisions and the reasoning behind them." }]
      confidence: 0.9 }
    { subject: "nur", predicate: "mentions"
      object: { kind: "entity", entity: "boru" }
      evidence: [{ source: "src:readme", locator: "Documentation",
        quote: "Non-Uniformity Register | You want the recorded deviations from the language's uniform rules, each pending, resolved, or explicitly allowed." }]
      confidence: 0.9 }
    { subject: "eng-guide", predicate: "supports"
      object: { kind: "entity", entity: "eng-go" }
      evidence: [{ source: "src:agents", locator: "Working in the code",
        quote: "Engine kernel — types, values, signatures, matching, the step loop, the parser bridge | eng/go/CLAUDE.md" }]
      confidence: 0.95 }
    { subject: "lang-guide", predicate: "supports"
      object: { kind: "entity", entity: "lang-go" }
      evidence: [{ source: "src:agents", locator: "Working in the code",
        quote: "Language layer — native words, modules, registry, help/describe, capabilities | lang/go/CLAUDE.md" }]
      confidence: 0.95 }
    { subject: "wpg", predicate: "supports"
      object: { kind: "entity", entity: "boru" }
      evidence: [{ source: "src:readme", locator: "Install",
        quote: "A wasm-powered browser playground is bundled in docs/index.html" }]
      confidence: 0.9 }
    { subject: "vault", predicate: "part_of"
      object: { kind: "entity", entity: "binary" }
      evidence: [{ source: "src:readme", locator: "Overview",
        quote: "a secrets vault" }]
      confidence: 0.95 }
    { subject: "vault-wire", predicate: "part_of"
      object: { kind: "entity", entity: "vault" }
      evidence: [{ source: "src:cli-md", locator: "The vault",
        quote: "HTTP wire protocol for secret provision" }]
      confidence: 0.95 }
    { subject: "lang-eng-audit", predicate: "part_of"
      object: { kind: "entity", entity: "design-notes" }
      evidence: [{ source: "src:audit", locator: "title",
        quote: "Lang/Eng Content Audit — and the Types-Module Proposal" }]
      confidence: 0.98 }
    { subject: "lang-eng-audit", predicate: "supports"
      object: { kind: "entity", entity: "eng-go" }
      evidence: [{ source: "src:audit", locator: "7A",
        quote: "the parser emits kernel STRUCTURAL MARKERS; the engine lowers each marker to word dispatches" }]
      confidence: 0.95 }
    { subject: "design-notes", predicate: "part_of"
      object: { kind: "entity", entity: "repo" }
      evidence: [{ source: "src:readme", locator: "Repository layout",
        quote: "Internal design notes and proposals." }]
      confidence: 0.9 }
    { subject: "content-addressing", predicate: "part_of"
      object: { kind: "entity", entity: "design-notes" }
      evidence: [{ source: "src:content-addressing", locator: "title",
        quote: "CONTENT-ADDRESSING.0 — identity by hash, and what it would actually take" }]
      confidence: 0.95 }
    { subject: "completeness-review", predicate: "part_of"
      object: { kind: "entity", entity: "design-notes" }
      evidence: [{ source: "src:completeness-review", locator: "title",
        quote: "Type checker + bytecode compiler — completeness review" }]
      confidence: 0.95 }
    { subject: "decl-grammar", predicate: "part_of"
      object: { kind: "entity", entity: "design-notes" }
      evidence: [{ source: "src:decl-grammar", locator: "title",
        quote: "DECLARATIVE-GRAMMAR.0 — one grammar artifact for both parser twins" }]
      confidence: 0.95 }
    # This used to point at eng-go-parser, the PACKAGE, back when the
    # parser was a package inside the eng/go module. It is now its own
    # top-level module, so gomod.boru emits parser-go from go.work and
    # never emits eng-go-parser at all — leaving the old key here would
    # dangle. The pair is still the point: code says where a unit sits
    # (parser-go depends_on core-go, from go.mod), docs say what it does.
    { subject: "decl-grammar", predicate: "supports"
      object: { kind: "entity", entity: "parser-go" }
      evidence: [{ source: "src:decl-grammar", locator: "The contract",
        quote: "parser/go/grammar.json is the single source of the boru grammar's STRUCTURE, loaded by both parsers" }]
      confidence: 0.95 }
    { subject: "coverage-parity", predicate: "part_of"
      object: { kind: "entity", entity: "design-notes" }
      evidence: [{ source: "src:coverage-parity", locator: "title",
        quote: "ENG-COVERAGE-PARITY.0 — the standalone 100%/100% program for the kernel twins" }]
      confidence: 0.95 }
    { subject: "coverage-parity", predicate: "supports"
      object: { kind: "entity", entity: "eng-go" }
      evidence: [{ source: "src:coverage-parity", locator: "The contract",
        quote: "eng proves itself with its OWN suite, on both implementations." }]
      confidence: 0.95 }
    { subject: "core-ts-coverage", predicate: "part_of"
      object: { kind: "entity", entity: "design-notes" }
      evidence: [{ source: "src:core-ts-coverage", locator: "title",
        quote: "CORE-TS-COVERAGE.0 — taking core/ts from 62% to 100%" }]
      confidence: 0.95 }
    { subject: "core-ts-coverage", predicate: "supports"
      object: { kind: "entity", entity: "core-go" }
      evidence: [{ source: "src:core-ts-coverage", locator: "The corpus is not the instrument",
        quote: "keep growing `core/spec` as a cross-engine spec, and stop treating it" }]
      confidence: 0.95 }
    { subject: "root-module", predicate: "part_of"
      object: { kind: "entity", entity: "design-notes" }
      evidence: [{ source: "src:root-module", locator: "title",
        quote: "ROOT-MODULE-FEASIBILITY.0 — measuring a shared module below core and parser" }]
      confidence: 0.95 }
    { subject: "root-module", predicate: "supports"
      object: { kind: "entity", entity: "core-go" }
      evidence: [{ source: "src:root-module", locator: "The finding",
        quote: "**The finding: `Value` alone closes over 75.7% of `core/go`.**" }]
      confidence: 0.95 }
    { subject: "basic-check-cut", predicate: "part_of"
      object: { kind: "entity", entity: "design-notes" }
      evidence: [{ source: "src:basic-check-cut", locator: "title",
        quote: "BASIC-CHECK-CUT.0 — removing `basic`'s dependency on `check`" }]
      confidence: 0.95 }
    { subject: "basic-check-cut", predicate: "supports"
      object: { kind: "entity", entity: "basic-go" }
      evidence: [{ source: "src:basic-check-cut", locator: "Status",
        quote: "**Status:** COMPLETE (2026-08-08)" }]
      confidence: 0.95 }
    { subject: "core-ts-divergences", predicate: "part_of"
      object: { kind: "entity", entity: "design-notes" }
      evidence: [{ source: "src:core-ts-divergences", locator: "title",
        quote: "CORE-TS-DIVERGENCES.1 — 135 measured core-level divergences, and where they hid" }]
      confidence: 0.95 }
    { subject: "core-ts-divergences", predicate: "supports"
      object: { kind: "entity", entity: "core-go" }
      evidence: [{ source: "src:core-ts-divergences", locator: "Why none of these were visible",
        quote: "An uncovered branch in one port is where a divergence hides" }]
      confidence: 0.95 }
    { subject: "go-ts-parity", predicate: "part_of"
      object: { kind: "entity", entity: "design-notes" }
      evidence: [{ source: "src:go-ts-parity", locator: "title",
        quote: "GO-TS-PARITY.0 — full functional parity on core, parser and basic" }]
      confidence: 0.95 }
    { subject: "go-ts-parity", predicate: "supports"
      object: { kind: "entity", entity: "parser-go" }
      evidence: [{ source: "src:go-ts-parity", locator: "The measurement",
        quote: "the parity gap is the uncovered surface" }]
      confidence: 0.95 }
    { subject: "ts-parity-audit", predicate: "part_of"
      object: { kind: "entity", entity: "design-notes" }
      evidence: [{ source: "src:ts-parity-audit", locator: "title",
        quote: "TS-PARITY-AUDIT.0 — the parser battery does not agree with parser/go" }]
      confidence: 0.95 }
    { subject: "ts-parity-audit", predicate: "supports"
      object: { kind: "entity", entity: "parser-go" }
      evidence: [{ source: "src:ts-parity-audit", locator: "the differential",
        quote: "After the fixes the differential is **0 divergences across 1,765 rows**." }]
      confidence: 0.95 }
    { subject: "adr-004-refinement", predicate: "part_of"
      object: { kind: "entity", entity: "design-notes" }
      evidence: [{ source: "src:adr-004-refinement", locator: "title",
        quote: "ADR-004 refinement — argument-handling categories" }]
      confidence: 0.95 }
    { subject: "adr-004-refinement", predicate: "supports"
      object: { kind: "entity", entity: "forward" }
      evidence: [{ source: "src:adr-004-refinement", locator: "Why it exists",
        quote: "ADR-004 describes a default but not a system." }]
      confidence: 0.95 }
    { subject: "canon-roundtrip", predicate: "part_of"
      object: { kind: "entity", entity: "design-notes" }
      evidence: [{ source: "src:canon-roundtrip", locator: "title",
        quote: "CANON-ROUNDTRIP — canon always round-trips" }]
      confidence: 0.95 }
    { subject: "canon-roundtrip", predicate: "supports"
      object: { kind: "entity", entity: "core-go" }
      evidence: [{ source: "src:canon-roundtrip", locator: "The contract",
        quote: "`canon v` renders a value as boru source text which, re-parsed," }]
      confidence: 0.95 }
    { subject: "type-representation-audit", predicate: "part_of"
      object: { kind: "entity", entity: "design-notes" }
      evidence: [{ source: "src:type-representation-audit", locator: "title",
        quote: "What a type name denotes — a representation audit" }]
      confidence: 0.95 }
    { subject: "type-representation-audit", predicate: "supports"
      object: { kind: "entity", entity: "core-go" }
      evidence: [{ source: "src:type-representation-audit", locator: "0. The three layers",
        quote: "The bug lives entirely in the third layer." }]
      confidence: 0.95 }
    { subject: "type-node-fusion", predicate: "part_of"
      object: { kind: "entity", entity: "design-notes" }
      evidence: [{ source: "src:type-node-fusion", locator: "title",
        quote: "One object, one seam — the type-node fusion design" }]
      confidence: 0.95 }
    { subject: "type-node-fusion", predicate: "supports"
      object: { kind: "entity", entity: "core-go" }
      evidence: [{ source: "src:type-node-fusion", locator: "3. The design",
        quote: "The design: a type IS its node, and the node carries its content" }]
      confidence: 0.95 }
    { subject: "state-machines", predicate: "part_of"
      object: { kind: "entity", entity: "design-notes" }
      evidence: [{ source: "src:state-machines", locator: "title",
        quote: "Design for general-purpose **state machines** in boru — a builtin" }]
      confidence: 0.95 }
    { subject: "state-machines", predicate: "supports"
      object: { kind: "entity", entity: "lang-go" }
      evidence: [{ source: "src:state-machines", locator: "Scope decisions (agreed)",
        quote: "State machines ship as the `boru:state` module" }]
      confidence: 0.95 }
    { subject: "hot-code-loading", predicate: "part_of"
      object: { kind: "entity", entity: "design-notes" }
      evidence: [{ source: "src:hot-code-loading", locator: "title",
        quote: "A report on boru's **hot code loading** ability" }]
      confidence: 0.95 }
    { subject: "hot-code-loading", predicate: "supports"
      object: { kind: "entity", entity: "lang-go" }
      evidence: [{ source: "src:hot-code-loading", locator: "2.3",
        quote: "Re-import **is** reload, today, for" }]
      confidence: 0.95 }
    { subject: "style-guide", predicate: "supports"
      object: { kind: "entity", entity: "boru" }
      evidence: [{ source: "src:readme", locator: "Documentation",
        quote: "Style Guide" }]
      confidence: 0.95 }
    { subject: "style-guide", predicate: "supports"
      object: { kind: "entity", entity: "binary" }
      evidence: [{ source: "src:style-guide", locator: "S1",
        quote: "boru fmt does not yet make this rule self-enforcing" }]
      confidence: 0.95 }
    { subject: "compile-declaration-model", predicate: "part_of"
      object: { kind: "entity", entity: "design-notes" }
      evidence: [{ source: "src:compile-declaration-model", locator: "title",
        quote: "The compile declaration model" }]
      confidence: 0.95 }
    { subject: "compile-declaration-model", predicate: "supports"
      object: { kind: "entity", entity: "compiler-go" }
      evidence: [{ source: "src:compile-declaration-model", locator: "3. Diagnosis: the flags are a patch set over a wrong default",
        quote: "The zero value is not \"no information\". It is a **substantive claim**" }]
      confidence: 0.95 }
    { subject: "full-compilation", predicate: "part_of"
      object: { kind: "entity", entity: "design-notes" }
      evidence: [{ source: "src:full-compilation", locator: "title",
        quote: "Full compilation — the total lowering design" }]
      confidence: 0.95 }
    { subject: "full-compilation", predicate: "supports"
      object: { kind: "entity", entity: "compiler-go" }
      evidence: [{ source: "src:full-compilation", locator: "0. The claim, in one paragraph",
        quote: "Under this design the worst verdict is *lower generically*" }]
      confidence: 0.95 }
    { subject: "full-compilation-handoff", predicate: "part_of"
      object: { kind: "entity", entity: "design-notes" }
      evidence: [{ source: "src:full-compilation-handoff", locator: "title",
        quote: "Full compilation — handoff for the bind-twin line" }]
      confidence: 0.95 }
    { subject: "full-compilation-handoff", predicate: "related_to"
      object: { kind: "entity", entity: "full-compilation" }
      evidence: [{ source: "src:full-compilation-handoff", locator: "Where the work is",
        quote: "What remains of Stage 4 is the bind twins" }]
      confidence: 0.95 }
    { subject: "session-handover", predicate: "part_of"
      object: { kind: "entity", entity: "design-notes" }
      evidence: [{ source: "src:session-handover", locator: "title",
        quote: "Session handover — the full-compilation project" }]
      confidence: 0.95 }
    { subject: "session-handover", predicate: "related_to"
      object: { kind: "entity", entity: "full-compilation-handoff" }
      evidence: [{ source: "src:session-handover", locator: "Purpose",
        quote: "Update this file at the end of every increment." }]
      confidence: 0.95 }
    { subject: "paren-restep-rule", predicate: "part_of"
      object: { kind: "entity", entity: "design-notes" }
      evidence: [{ source: "src:paren-restep-rule", locator: "title",
        quote: "The paren re-step rule" }]
      confidence: 0.95 }
    { subject: "paren-restep-rule", predicate: "supports"
      object: { kind: "entity", entity: "compiler-go" }
      evidence: [{ source: "src:paren-restep-rule", locator: "1. The rule",
        quote: "exactly when it leads **two or more survivors**" }]
      confidence: 0.95 }
    { subject: "higher-order-functions", predicate: "part_of"
      object: { kind: "entity", entity: "design-notes" }
      evidence: [{ source: "src:higher-order-functions", locator: "title",
        quote: "Higher-Order Functions in boru" }]
      confidence: 0.95 }
    { subject: "higher-order-functions", predicate: "supports"
      object: { kind: "entity", entity: "core-go" }
      evidence: [{ source: "src:higher-order-functions", locator: "3. The one rule that explains most of the surprises",
        quote: "That rule is coherent, and it is the source of every" }]
      confidence: 0.95 }
    { subject: "function-value-scope", predicate: "part_of"
      object: { kind: "entity", entity: "design-notes" }
      evidence: [{ source: "src:function-value-scope", locator: "title",
        quote: "Where a function value's **free words** resolve" }]
      confidence: 0.95 }
    { subject: "function-value-scope", predicate: "supports"
      object: { kind: "entity", entity: "core-go" }
      evidence: [{ source: "src:function-value-scope", locator: "1. Summary",
        quote: "The mechanism for the correct behaviour already exists and is already" }]
      confidence: 0.95 }
    { subject: "reload-invalidation", predicate: "part_of"
      object: { kind: "entity", entity: "design-notes" }
      evidence: [{ source: "src:reload-invalidation", locator: "title",
        quote: "How **hot code reloading** and **transparent compilation** coexist without" }]
      confidence: 0.95 }
    { subject: "reload-invalidation", predicate: "supports"
      object: { kind: "entity", entity: "compiler-go" }
      evidence: [{ source: "src:reload-invalidation", locator: "5. The design",
        quote: "replace the per-invoke DepSnap walk with a per-ref valid" }]
      confidence: 0.95 }
    { subject: "boru-viz", predicate: "part_of"
      object: { kind: "entity", entity: "design-notes" }
      evidence: [{ source: "src:boru-viz", locator: "title",
        quote: "boru:viz — diagram source generation from arbitrary data structures" }]
      confidence: 0.95 }
    { subject: "boru-viz", predicate: "supports"
      object: { kind: "entity", entity: "lang-go" }
      evidence: [{ source: "src:boru-viz", locator: "Architecture — what is new",
        quote: "`lang/go/modules/viz.go` — `BuildVizModule` scaffold" }]
      confidence: 0.95 }
    { subject: "boru-scry", predicate: "part_of"
      object: { kind: "entity", entity: "design-notes" }
      evidence: [{ source: "src:boru-scry", locator: "title",
        quote: "boru:scry — a boru system's knowledge of itself, as plain data" }]
      confidence: 0.95 }
    { subject: "boru-scry", predicate: "supports"
      object: { kind: "entity", entity: "lang-go" }
      evidence: [{ source: "src:boru-scry", locator: "Architecture",
        quote: "File layout: `lang/go/modules/scry.go`, `docs_scry.go`," }]
      confidence: 0.95 }
    { subject: "go-module-graph", predicate: "part_of"
      object: { kind: "entity", entity: "design-notes" }
      evidence: [{ source: "src:go-module-graph", locator: "title",
        quote: "Go module graph and per-module coverage" }]
      confidence: 0.95 }
    { subject: "go-module-graph", predicate: "related_to"
      object: { kind: "entity", entity: "kg" }
      evidence: [{ source: "src:go-module-graph", locator: "Status",
        quote: "the same ground truth `kg/gomod.boru` reads to build the knowledge graph's module view" }]
      confidence: 0.95 }
    { subject: "module-views", predicate: "part_of"
      object: { kind: "entity", entity: "design-notes" }
      evidence: [{ source: "src:module-views", locator: "title",
        quote: "Design for **module-provided views and widgets** in boru" }]
      confidence: 0.95 }
    { subject: "module-views", predicate: "supports"
      object: { kind: "entity", entity: "lang-go" }
      evidence: [{ source: "src:module-views", locator: "Context",
        quote: "the renderer's kind set is **closed**" }]
      confidence: 0.95 }
    { subject: "boru-infoview", predicate: "part_of"
      object: { kind: "entity", entity: "design-notes" }
      evidence: [{ source: "src:boru-infoview", locator: "title",
        quote: "boru infoview — the stack at the cursor" }]
      confidence: 0.95 }
    { subject: "boru-infoview", predicate: "supports"
      object: { kind: "entity", entity: "cmd-go" }
      evidence: [{ source: "src:boru-infoview", locator: "Architecture — what is new",
        quote: "`cmd/go/internal/lsp/` — inlay hints, hover, code actions, `boru/stackAt`" }]
      confidence: 0.95 }
    { subject: "boru-infoview", predicate: "supports"
      object: { kind: "entity", entity: "lang-go" }
      evidence: [{ source: "src:boru-infoview", locator: "Architecture — what is new",
        quote: "`lang/go/checktrace.go` — the `CheckTrace` position→stack oracle" }]
      confidence: 0.95 }
  ]
}

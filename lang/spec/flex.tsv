# tokens	expected	note
# =====================================================================		
# flex nodes — FlexMap and FlexList		
# =====================================================================		
# FlexMap (Node/Map/FlexMap) and FlexList (Node/List/FlexList) are		
# MUTABLE child types of Map and List. `set` and the list-mutation		
# family (append/push/pop/unshift/shift) change a flex node IN PLACE		
# and return the node itself (pop/shift additionally return the		
# removed element on top). Everything else — get, each, sort, is,		
# typeof, deq, sig dispatch into Map/List slots — is inherited from		
# the immutable parents.		
#		
# Construction: `flex <node>` (sugar) or `make FlexMap m` /		
# `make FlexList l`; both DEEP-COPY, so mutation never leaks into the		
# immutable source. The inverse `node <val>` (= `make Map v` /		
# `make List v`) deep-converts back to immutable Map/List.		
#		
# Canon renders flex nodes round-trippable: `(flex {…})` / `(flex […])`.		
#		
# This file covers:		
#   1. Construction, typeof, rendering.		
#   2. make symmetry and conversion (FlexMap/FlexList/Map/List).		
#   3. In-place set, aliasing, chaining.		
#   4. Deep-copy isolation (no mutation leaks, either direction).		
#   5. FlexList set bounds — sparse is an error.		
#   6. append (element, list-concat, wrapped-list).		
#   7. push/pop/unshift/shift — in place on flex, copying on plain.		
#   8. node — deep immutable conversion, round trip.		
#   9. Equality and ordering across flex/plain.		
#  10. is / unify — nominal subtype rules.		
#  11. Inherited non-mutating words (results are plain — by design).		
		
# ====================================================================		
# 1. Construction, typeof, rendering		
# ====================================================================		
		
typeof (flex {})	FlexMap	flex on a map yields a FlexMap
typeof (flex [])	FlexList	flex on a list yields a FlexList
flex {a:1}	(flex {a:1})	canon renders flex maps round-trippable
flex [1 2]	(flex [1 2])	canon renders flex lists round-trippable
flex {a:(1 add 2)}	(flex {a:3})	map values evaluate before the copy
flex [1 add 2]	(flex [3])	list bodies auto-evaluate before the copy
flex 1	ERROR:signature	scalars are not flexable
flex 'x'	ERROR:signature	strings are not flexable
flex Map	ERROR:signature	bare type literal rejected — needs a concrete node
flex List	ERROR:signature	bare type literal rejected
typeof ((flex {a:[1]}).a)	FlexList	flex conversion is deep — nested list becomes flex
typeof ((flex [{a:1}]) get 0)	FlexMap	deep in the list direction too
		
# ====================================================================		
# 2. make symmetry and conversion		
# ====================================================================		
		
make FlexMap {}	(flex {})	make constructs a FlexMap
make FlexList []	(flex [])	make constructs a FlexList
typeof (make FlexMap {a:1})	FlexMap	
typeof (make FlexList [1])	FlexList	
make Map {}	{}	symmetric: make Map works (same as the literal)
make List []	[]	symmetric: make List works
typeof (make Map {a:1})	Map	
typeof (make List [1])	List	
make FlexMap (flex {a:1})	(flex {a:1})	an existing flex node copies
make FlexMap {a:1}	(flex {a:1})	any existing Node with data flexes
make FlexList [1 2]	(flex [1 2])	
make Map (flex {a:1})	{a:1}	make Map is the inverse — back to immutable
make List (flex [1 2])	[1 2]	
typeof (make Map (flex {a:1}))	Map	
typeof ((make Map (flex {a:[1]})) dot a/q)	List	inverse conversion is deep — nested flex gone
make FlexMap [1 2]	ERROR:make	a list is not a map — family mismatch
make FlexList {a:1}	ERROR:make	a map is not a list
make FlexMap 1	ERROR:make	scalars have no flex version
make Map [1 2]	ERROR:make	make Map needs a map-family source
def R (refine Record [ { a:Integer } ]) make R (flex { a:5 })	{a:5}	record instantiation accepts a flex source
def R (refine Record [ { a:Integer } ]) make R { a:5 }	{a:5}	regression: plain record make is unchanged
		
# ====================================================================		
# 3. In-place set, aliasing, chaining		
# ====================================================================		
		
set b/q 2 (flex {a:1})	(flex {a:1 b:2})	set mutates and returns the node
set 'b' 2 (flex {a:1})	(flex {a:1 b:2})	string key form
set a/q 9 (flex {a:1})	(flex {a:9})	overwrite an existing key
def f (flex {a:1}) set b/q 2 f drop f	(flex {a:1 b:2})	the binding sees the mutation — same node
def f (flex {}) def g f set a/q 1 g drop f	(flex {a:1})	two bindings share one node
set c/q 3 (set b/q 2 (flex {a:1}))	(flex {a:1 b:2 c:3})	returned node chains into the next set
set 1 99 (flex [1 2 3])	(flex [1 99 3])	integer index set on a FlexList
set 0 9 (flex [1])	(flex [9])	first element
def m {a:{b:1}} def f (flex m) set b/q 9 f.a drop f.a.b	9	nested flex maps are mutable too (deep flex)
		
# ====================================================================		
# 4. Deep-copy isolation — mutation never leaks		
# ====================================================================		
		
def m {a:1} def f (flex m) set a/q 9 f drop m.a	1	mutating the flex copy leaves the source map untouched
def m {a:{b:1}} def f (flex m) set b/q 9 f.a drop m.a.b	1	deep: nested source maps are isolated too
def f (flex {a:1}) def n (node f) set a/q 9 f drop n.a	1	a node snapshot is isolated from later mutation
def l [1 2] def f (flex l) push 9 f drop l	[1 2]	list sources are isolated as well
		
# ====================================================================		
# 5. Plain Map/List stay immutable; FlexList bounds		
# ====================================================================		
		
set a/q 1 {a:0}	{a:1}	set on a plain Map is COPY-RETURNING (a new map; see maps spec)
def m {a:0} (set a/q 1 m) drop m	{a:0}	…the receiver itself is untouched — Node immutability is intact
set 0 9 [1 2]	[9 2]	set on a plain List is copy-returning too
def l [1 2] (set 0 9 l) drop l	[1 2]	…and the receiver list is untouched
set 3 99 (flex [1 2 3])	ERROR:out of bounds	index == length is out of bounds — use append
set 9 99 (flex [1 2 3])	ERROR:out of bounds	sparse FlexLists are an error — never pad with None
set 0 9 (flex [])	ERROR:out of bounds	empty list has no slot 0
		
# ====================================================================		
# 6. append		
# ====================================================================		
		
append 4 (flex [1 2])	(flex [1 2 4])	append one element
append 'x' (flex [])	(flex ['x'])	append to empty
append [3 4] (flex [1 2])	(flex [1 2 3 4])	a list argument concatenates its elements
append (flex [3]) (flex [1 2])	(flex [1 2 3])	a FlexList argument concatenates too
append [[3 4]] (flex [1 2])	(flex [1 2 (flex [3 4])])	wrap a list to append it AS one element — adopted to flex (entirely-mutable tree)
append {a:1} (flex [1])	(flex [1 (flex {a:1})])	non-list Node values append as elements — adopted to flex
def l (flex [1 2]) append l l	(flex [1 2 1 2])	self-concat snapshots before growing
append 4 (flex [1]) append 5	(flex [1 4 5])	returned node chains
append 1 [1 2]	ERROR:signature	append needs a FlexList — plain lists are immutable
append 1 {a:1}	ERROR:signature	append does not apply to maps
		
# ====================================================================		
# 7. push / pop / unshift / shift		
# ====================================================================		
		
push 3 (flex [1 2])	(flex [1 2 3])	push mutates in place and returns the node
def l (flex [1 2]) push 3 l drop l	(flex [1 2 3])	the binding sees the push
pop (flex [1 2])	(flex [1]) 2	pop returns the node and the removed element
def l (flex [1 2]) pop l drop drop l	(flex [1])	the binding sees the pop
unshift 0 (flex [1 2])	(flex [0 1 2])	
shift (flex [1 2])	(flex [2]) 1	
pop (flex [])	ERROR:pop	cannot pop an empty FlexList
shift (flex [])	ERROR:shift	cannot shift an empty FlexList
def l [1 2] push 3 l drop l	[1 2]	plain lists keep copy semantics — original unchanged
push 3 [1 2]	[1 2 3]	plain push still returns a new plain list
typeof (push 3 [1 2])	List	
pop [1 2]	[1] 2	plain pop unchanged
		
# ====================================================================		
# 8. node — deep immutable conversion		
# ====================================================================		
		
node (flex {a:1})	{a:1}	flex map back to immutable map
node (flex [1 2])	[1 2]	flex list back to immutable list
typeof (node (flex {a:1}))	Map	
typeof (node (flex [1]))	List	
typeof ((node (flex {a:[1]})) dot a/q)	List	deep: nested flex converted too
node {a:1}	{a:1}	identity on an already-plain map
node [1 2]	[1 2]	identity on a plain list
node 1	ERROR:signature	scalars are not nodes
node Map	ERROR:signature	bare type literal rejected
		
# ====================================================================		
# 8b. Truthiness — empty flex containers are falsey, like plain ones		
# ====================================================================		
		
not (flex [])	true	empty FlexList is falsey
not (flex {})	true	empty FlexMap is falsey
not (flex [1])	false	non-empty FlexList is truthy
not (flex {a:1})	false	non-empty FlexMap is truthy
if (flex {}) [1] [2]	2	empty flex map takes the else branch
if (flex [9]) [1] [2]	1	non-empty flex list takes the then branch
not []	true	parity: plain empties are falsey the same way
not {}	true	
		
# ====================================================================		
# 9. Equality and ordering		
# ====================================================================		
		
(flex {a:1}) deq {a:1}	true	deep equality ignores flexness
{a:1} deq (flex {a:1})	true	both directions
(flex [1 [2]]) deq [1 [2]]	true	deep — nested flex lists compare by content
(flex {a:1}) deq {a:2}	false	different content still differs
(flex [1]) eq (flex [1])	false	eq is container identity — two distinct nodes
def f (flex [1]) f eq f	true	same node is eq to itself
def f (flex {a:1}) def g f f eq g	true	aliases share the container
(flex [1 2]) cmp [1 2]	0	cmp normalises flexness — equal content orders equal
[1 2] cmp (flex [1 3])	-1	content ordering still applies
List tcmp FlexList	-1	bare type literals keep their lattice Rank order
		
# ====================================================================		
# 10. is / unify — nominal subtype rules		
# ====================================================================		
		
(flex {}) is FlexMap	true	
(flex {}) is Map	true	a FlexMap IS a Map (child type)
(flex {}) is Node	true	lattice transitivity: FlexMap < Map < Node
(flex []) is Node	true	
{} is Node	true	plain containers are Nodes too
[] is Node	true	
1 is Node	false	scalars are not Nodes
Node unify (flex [1 2])	(flex [1 2]) true	the Node literal accepts and keeps a flex value
Node unify {a:1}	{a:1} true	
Node unify 1	'~unify-fail' false	no scalar passes the Node literal
{} is FlexMap	false	a plain map is NOT a FlexMap
(flex []) is FlexList	true	
(flex []) is List	true	
[] is FlexList	false	
(flex [1 2]) unify [1 2]	[1 2] true	unify output is a plain node (by design)
List unify (flex [1 2])	(flex [1 2]) true	the List literal accepts a flex value and keeps it
		
# ====================================================================		
# 11. Inherited non-mutating words — results are PLAIN (by design)		
# ====================================================================		
		
(flex [10 20]) get 1	20	indexed get
(flex {a:7}).a	7	dot access
(flex {a:7}) dot a/q	7	keyed get
size (flex [1 2 3])	3	
sort (flex [3 1 2])	[1 2 3]	sort returns a new PLAIN list
typeof (sort (flex [3 1 2]))	List	
reverse (flex [1 2])	[2 1]	
each [ add 1 ] (flex [1 2 3])	[2 3 4]	higher-order traversal works unchanged
#
# §12  writes keep trees ENTIRELY one column (the adoption rule).
# Storing a plain Node INTO a flex container deep-flexes it — otherwise
# the tree goes mixed and a later inner write is copy-returning and
# silently lost. Storing a flex value INTO a plain Map/List snapshots
# it to its immutable shape — an "immutable" container must never
# change underneath through a live handle. A flex value stored into a
# FLEX container stays a SHARED handle (sharing what the caller
# explicitly passes, like class-default provided values).
def f (flex {}) set a {b:1} f end typeof f.a	(flex {a:(flex {b:1})}) FlexMap	a plain map stored into a FlexMap becomes flex (set returns the node for chaining)
def f (flex {}) set a {b:1} f end set b 9 f.a end f.a.b	(flex {a:(flex {b:9})}) (flex {b:9}) 9	…so the nested write STICKS (was silently lost on a mixed tree)
def f (flex []) push {x:1} f end typeof f.0	(flex [(flex {x:1})]) FlexMap	push adopts a plain map element
def f (flex []) push {x:1} f end set x 9 (f get 0) end (f get 0).x	(flex [(flex {x:9})]) (flex {x:9}) 9	…and the pushed element is writable in place
def f (flex []) append {x:1} f end typeof f.0	(flex [(flex {x:1})]) FlexMap	append (single element) adopts
def f (flex []) append [{a:1} [2]] f end typeof f.1	(flex [(flex {a:1}) (flex [2])]) FlexList	append (list splat) adopts every element
def f (flex []) unshift {x:1} f end typeof f.0	(flex [(flex {x:1})]) FlexMap	unshift adopts
def f (flex [0]) set 0 {b:1} f end typeof f.0	(flex [(flex {b:1})]) FlexMap	indexed FlexList set adopts
def f (flex {}) set a "s" f end f.a	(flex {a:'s'}) 's'	scalars pass through untouched
def h (flex {z:1}) def f (flex {}) set a h f end set z 9 f.a end h.z	(flex {a:(flex {z:9})}) (flex {z:9}) 9	a FLEX value stored into flex stays a shared handle — both views mutate
def iv (flex {b:1}) typeof ({} set a iv).a	Map	a flex value stored into a PLAIN map is snapshot to its immutable shape
def iv (flex {b:1}) def m ({} set a iv) set b 9 iv end m.a.b	(flex {b:9}) 1	…so mutating the handle afterwards does NOT change the "immutable" map
def iv (flex [1]) typeof (push iv []).0	List	plain-list push snapshots a flex element
def iv (flex [1]) typeof (unshift iv []).0	List	plain-list unshift snapshots too

# ====================================================================
# 12. del — removal across every container `set` writes
# ====================================================================
# `del key container` completes the storage column rule `set`
# established, and dispatches over EXACTLY the containers `set` does
# (NUR022). Each one either removes the slot or says why it will not:
#
#   Map                    copy-returning — a NEW map without the key
#   FlexMap / WeakFlexMap  in place, returns the node (aliases observe)
#   FlexXml / WeakFlexXml  removes an ATTRIBUTE, the slot set writes
#   Store                  copy-on-write, via a tombstone layer
#   Class                  declined — a declared field is sealed
#   Micron                 declined — immutable, like set's form
#   List / FlexList /      declined — index removal SHIFTS the tail, so
#     WeakFlexList         it is not set's inverse; pop / shift /
#                          ArrayUtil.remove-at do that
#
# The compile failures are registered signatures, not sig-absence, so each
# raises its own message instead of an opaque dispatch failure. A
# missing key is a no-op wherever removal is supported. Keys are
# strings or atoms; computed keys via parens.
#
# `del` is NOT `set key none`: an absent key and a key bound to none
# are distinguishable (`has`, `size`, `keys`, and container `eq` all
# tell them apart), so the two writes produce different containers.

def f (flex {a:1 b:2}) (f del "a")	(flex {b:2})	FlexMap del removes the key in place
def f (flex {a:1 b:2}) ((f del "a") eq f)	true	…and returns the receiver node itself
def f (flex {a:1 b:2 c:3}) f del a del b	(flex {c:3})	chained deletes keep operating on one node (atom keys quote)
def f (flex {a:1}) def g f (f del "a") drop g	(flex {})	every alias observes the in-place removal
def f (flex {a:1}) ((f del "zz") eq f)	true	missing key: no-op, same node back
def f (flex {a:none}) (f del "a") size	0	a present-but-none entry deletes like any other
def k "b" (flex {a:1 b:2}) del (k)	(flex {a:1})	computed key via parens
{a:1 b:2} del a	{b:2}	Map del is copy-returning
def m {a:1 b:2} (m del "a") drop m	{a:1 b:2}	…and leaves the receiver untouched
{a:1} del zz	{a:1}	Map del of a missing key: unchanged copy
del "a" 5	ERROR:signature	scalars have no del
del "a" [1 2]	ERROR:signature	a list is indexed, not keyed — a String key matches no sig
del "a" (flex [1 2])	ERROR:signature	same for a FlexList
del 0 {a:1}	ERROR:signature	an integer key is not a map key

# --- WeakFlexMap: in place, like FlexMap (the weak payload needs its own
# --- handler, not because deletion differs but because AsMutableMap
# --- declines the payload by design)
def w (make WeakFlexMap {a:1 b:2}) (w del "a")	(make WeakFlexMap {b:2})	WeakFlexMap del removes the key in place
def w (make WeakFlexMap {a:1 b:2}) ((w del "a") eq w)	true	…and returns the receiver node
def w (make WeakFlexMap {a:1}) ((w del "zz") eq w)	true	missing key: no-op
def w (make WeakFlexMap {a:1 b:2}) (w del a) size	1	atom keys quote here too

# --- FlexXml / WeakFlexXml: del removes an ATTRIBUTE — the slot set writes
def x (flex <a b='1' c='2'/>) (x del "b")	<a c="2"/>	FlexXml del removes the attribute
def x (flex <a b='1'>hi</a>) (x del "b")	<a>hi</a>	children are untouched — they are not attributes
def x (flex <a b='1'/>) ((x del "zz") eq x)	true	missing attribute: no-op, same node back
def x (flex <a/>) ((x del "zz") eq x)	true	no attributes at all: still a no-op
def x (flex <a b='1' c='2'/>) def w (make WeakFlexXml x) (w del "b")	(make WeakFlexXml <a c="2"/>)	weakness does not change attribute deletion

# --- Store: copy-on-write, mirroring set's Store form (no stack result)
context set 'k' 7 end context del 'k' end context has 'k'	false	Store del removes the binding
context set 'k' 7 end context del 'k' end context get 'k'	ERROR:unknown key	…and the strict reader misses, as for a key never set
context del 'zz' end context has 'zz'	false	deleting an unbound key is a no-op
context set 'k' 1 end context del 'k' end context set 'k' 2 end context get 'k'	2	a set after a del re-binds in the same layer
context set 'k' 1 end context del 'k' end context set 'k' 2 end (context get 'k') add 1	3	…and the re-bound value is USABLE: del's forget-marker must be gradual, not strict Any, or every typed use of the key declines
# The container twin of the row above — `def l [(context get 'k')] (l get 0)
# add 1` — moved to the frontier ledger (frontier-context-inline.tsv): a
# `context` read inside an auto-evaluated list is an inline-lowered region
# with no compiled context layer, so it declines compilation (NUR054).
# The layer-local half — a tombstone hides an INHERITED key without touching
# the layer that owns it — needs a scope-pushing word, and every one of those
# is a code-body word the compiler declines, so it cannot live in this corpus.
# eng/go/store_tombstone_test.go covers it directly.

# --- compile failures: each names its own reason rather than failing opaquely
def P (class {n:Integer}) def p (make P {n:1}) (p del "n")	ERROR:sealed	a declared class field cannot be removed
(make Pathon "/x") del "kind"	ERROR:immutable	Micron del mirrors set's immutability compile failure
[1 2] del 0	ERROR:cannot remove an element	index removal shifts the tail — not set's inverse
(flex [1 2]) del 0	ERROR:cannot remove an element	same for a FlexList
(make WeakFlexList [1 2]) del 0	ERROR:cannot remove an element	…and a WeakFlexList

# --- del is not `set key none`: the absent/present-none distinction is real
{a:1 b:2} del b	{a:1}	del removes the key
{a:1 b:2} set b none	{a:1 b:none}	set none KEEPS the key, bound to none
({a:1 b:2} del b) eq ({a:1 b:2} set b none)	false	so the two writes differ
({a:1} has 'b') eq ({a:1 b:none} has 'b')	false	`has` is what tells them apart
{a:1 b:none} size	2	…and so does size — a none-bound key is a key

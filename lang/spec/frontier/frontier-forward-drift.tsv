# L-EACH — forward operand accounting across a dynamic/island residual (plan
# Phase 5; engine.go refuseForwardStackDrift). The over-widening guards (the
# five compile-negatives) stay in lang/go/bytecode_edge_findings_test.go.
# GRADUATED 2026-07-14: all three rows compile natively — errorReturnsFn
# narrows the catch result to dynamic(join(pass-through, handler-residual)),
# making the drifting String catch-all disjoint, so check mode selects the
# interpreter's forward collection (rows moved to bytecode-migrated.tsv;
# the genuinely-wide-join negative `5 do [7] error ["x"] add 1` keeps the
# drift compile failure, pinned in bytecode_edge_findings_test.go).

# `do … error` whose handler nets NO value, followed by more program —
# the P1.6 capability target (checker-compiler-completeness-review.0.md
# §8.2(6); COMPILE-REFUSAL-SURVEY.0.md §3d) — GRADUATED 2026-08-03 (review
# §9.13) for the PROVEN-raise shape: a strict Error do-result makes the
# pass-through arm statically dead, so the zero is a FIXED arity —
# errorReturnsFn returns it truthfully, the dispatch records a 0-output
# call, and stripResidualShapeOK's want-0 arm admits the empty handler
# residual (no island, no new opcode; the row moved to the main corpus and
# is pinned native in lang/go/bytecode_do_error_arity_test.go). The
# The MAYBE-raising half GRADUATED 2026-09-10 (the forty-eighth increment),
# and it needed no new opcode either. The variable arity is a runtime-
# variadic REGION — the device await's winner and a value-producing loop
# already ride — and the island's ONE simulated slot IS that
# representation: runFallback appends whatever the re-run produced, 0
# values or 1. So errorReturnsFn returns the honest spread instead of
# marking the program uncompilable, RecordFallback marks the event a
# region, and the residual absorbs the run. The rows moved to
# lang/spec/bytecode-migrated.tsv. This file is now empty of ledgered rows.

# boru Language Specification: the boru:vault-tui module (the vault TUI app)
# Format: input<TAB>expected<TAB>description
#
# The interactive vault TUI written in boru (lang/go/modules/vault_tui.boru,
# design/VAULT-TUI-PORT.0.md). The module exports the app map and its
# runner under the `VaultTui` namespace:
#
#   VaultTui.app  — the {init update view} app map
#   VaultTui.run  — run it under Tui.run against the registered backends
#
# The spec harness registers NO terminal and NO vault backend, so the
# module loads (it only defines words), the app map builds, and `run`
# pins the first-class no_backend error. Live sessions run in Go against
# a VirtualBackend + scripted vault (lang/go/test/app_vault_tui_test.go).
#
# ─────────────────────────────────────────────────────────────────────
# §1  import binds the VaultTui namespace
import "boru:vault-tui" convert List VaultTui.$module	['VaultTui']	import binds the VaultTui namespace
#
# §2  the app map builds headless: init state, update fn, view fn
import "boru:vault-tui"  join "," (keys (VaultTui.app {}))	'init,update,view'	VaultTui.app is the {init update view} map
#
# §3  no terminal backend registered → run declines with no_backend
import "boru:vault-tui"  VaultTui.run {}	ERROR:no terminal backend registered	VaultTui.run requires a registered terminal backend
